{{ define "models-go" }}
// Code generated by goapi. DO NOT EDIT.
{{ include "package" . }}

{{- range $key, $schema := .Doc.Components.Schemas }}

// {{ $key }}{{ with $schema.Description }}: {{ . }}{{ end }}
type {{ $key }} {{ include "schema-type" (dict "schema" $schema "required" true "components" $.Doc.Components) }}
{{- end }}
{{- end }}

{{ define "go-safe-field-name" }}
{{- regexReplaceAll ":" . "_" | toGoPascalCase }}
{{- end }}

{{ define "schema-type" }}
{{- $schema := get . "schema" }}
{{- $reqd := get . "required" }}
{{- $tag := get . "tag" }}
{{- $components := get . "components" }}
{{- $ptr := not $reqd }}
{{- with $schema }}
  {{- with get .Extensions "x-go-skip-optional-pointer" }}
    {{- $ptr = false }}
  {{- end }}
  {{- with get .Extensions "x-go-type" }}
    {{- if $ptr }}*{{ end }}{{ . }}
  {{- else }}
    {{- if eq .Type "string" }}{{ include "string-type" (dict "schema" $schema "required" $reqd) }}
    {{- else if eq .Type "integer" }}{{ include "integer-type" (dict "schema" $schema "required" $reqd) }}
    {{- else if eq .Type "number" }}{{ include "number-type" (dict "schema" $schema "required" $reqd) }}
    {{- else if eq .Type "boolean" }}{{ include "boolean-type" (dict "schema" $schema "required" $reqd) }}
    {{- else if eq .Type "object" }}{{ include "object-type" (dict "schema" $schema "required" $reqd "components" $components) }}
    {{- else if eq .Type "array" }}{{ include "array-type" (dict "schema" $schema "required" $reqd "components" $components) }}
    {{- else if .AllOf }}{{ include "object-allof-type" (dict "schema" $schema "required" $reqd "components" $components) }}
    {{- else }}any
    {{- end }}
  {{- end }}
{{- else }}any
{{- end }}
{{- end }}


{{ define "object-type" }}
{{- $schema := get . "schema" }}
{{- $reqd := get . "required" }}
{{- $components := get . "components" }}
{{- $ptr := not $reqd }}
{{- with get $schema.Extensions "x-go-skip-optional-pointer" }}
{{- $ptr = false }}
{{- end }}
{{- $requiredFields := $schema.Required }}
{{- with $schema.Properties }}
{{- if not $reqd }}*{{ end }}struct {
  {{- range $key, $item := . }}
  {{- $childReqd := has $key $requiredFields }}
  {{- include "object-field" (dict "key" $key "ref" $item "required" $childReqd "extensions" $schema.Extensions "components" $components ) }}
  {{- end }}
}
{{- else }}
{{- if $ptr }}*{{ end }}map[string]interface{}
{{- end }}
{{- end }}

{{ define "object-field" }}
{{- $key := get . "key" }}
{{- $ref := get . "ref" }}
{{- $required := get . "required" }}
{{- $extensions := get . "extensions" }}
{{- $components := get . "components" }}
{{- $fieldName := include "go-safe-field-name" $key }}
{{- with $ref.Value }}
{{- with get .Extensions "x-go-name" }}
{{- $fieldName = . }}
{{- end }}
{{- end }}
{{- $tags := list }}
{{- if get $extensions "x-goapi-query-schema" }}
{{- $tags = append $tags (include "query-tag" (dict "key" $key)) }}
{{- else }}
{{- $tags = append $tags (include "json-tag" (dict "key" $key "required" $required)) }}
{{- end }}
{{- $tags = append $tags (include "validate-tag" (dict "ref" $ref "required" $required "components" $components)) }}
{{- with $ref.Value }}
{{- with .Description }}

// {{ . }}
{{- end }}
{{- end }}
{{ $fieldName }} {{ include "ref-type" (dict "ref" $ref "required" $required "tag" $key "components" $components) }} `{{ $tags | join " " }}`
{{- end }}

{{ define "object-allof-type" }}
{{- $schema := get . "schema" }}
{{- $reqd := get . "required" }}
{{- $components := get . "components" }}
{{- if not $reqd }}*{{ end }}struct {
  {{- range $schema.AllOf }}
  {{- if .Ref }}
  {{ include "ref-type" (dict "ref" . "required" true "components" $components) }}
  {{- else }}
  {{- $requiredFields := .Value.Required }}
  {{- with .Value.Properties }}
  {{- range $key, $item := . }}
  {{- $childReqd := has $key $requiredFields }}
  {{- include "object-field" (dict "key" $key "ref" $item "required" $childReqd "extensions" $schema.Extensions "components" $components ) }}
  {{- end }}
  {{- end }}
  {{- end }}
  {{- end }}
}
{{- end }}

{{ define "array-type" }}
{{- $schema := get . "schema" }}
{{- $reqd := get . "required" }}
{{- $components := get . "components" }}
{{- $ptr := not $reqd }}
{{- with get $schema.Extensions "x-go-skip-optional-pointer" }}
{{- $ptr = false }}
{{- end }}
{{- if $ptr }}*{{ end }}
{{- with $schema.Items }}[]{{ include "ref-type" (dict "ref" . "required" true "components" $components) }}
{{- else }}interface{}
{{- end }}
{{- end }}

{{ define "string-type" }}
{{- $schema := get . "schema" }}
{{- $reqd := get . "required" }}
{{- $ptr := not $reqd }}
{{- with get $schema.Extensions "x-go-skip-optional-pointer" }}
{{- $ptr = false }}
{{- end }}
{{- if eq $schema.Format "date-time" }}
{{- if $ptr }}*{{ end }}time.Time
{{- else if eq $schema.Format "duration" }}
{{- if $ptr }}*{{ end }}Duration
{{- else }}
{{- if $ptr }}*{{ end }}string
{{- end }}
{{- end }}

{{ define "number-type" }}
{{- $schema := get . "schema" }}
{{- $reqd := get . "required" }}
{{- $ptr := not $reqd }}
{{- with get $schema.Extensions "x-go-skip-optional-pointer" }}
{{- $ptr = false }}
{{- end }}
{{- if $ptr }}*{{ end }}float64
{{- end }}

{{ define "integer-type" }}
{{- $schema := get . "schema" }}
{{- $reqd := get . "required" }}
{{- $ptr := not $reqd }}
{{- with get $schema.Extensions "x-go-skip-optional-pointer" }}
{{- $ptr = false }}
{{- end }}
{{- if $ptr }}*{{ end }}int
{{- end }}

{{ define "boolean-type" }}
{{- $schema := get . "schema" }}
{{- $reqd := get . "required" }}
{{- $ptr := not $reqd }}
{{- with get $schema.Extensions "x-go-skip-optional-pointer" }}
{{- $ptr = false }}
{{- end }}
{{- if $ptr }}*{{ end }}bool
{{- end }}

{{ define "ref-type" }}
{{- $ref := get . "ref" }}
{{- $reqd := get . "required" }}
{{- $tag := get . "tag" }}
{{- $components := get . "components" }}
{{- if eq $ref.Ref "" }}{{ include "schema-type" (dict "schema" $ref.Value "required" $reqd "tag" $tag "components" $components) }}
{{- else }}
{{- if not $reqd }}*{{ end }}{{ splitList "/" $ref.Ref | last }}
{{- end }}
{{- end }}

{{ define "json-tag" }}
{{- $key := get . "key" }}
{{- $reqd := get . "required" }}json:"{{ $key }}{{- if not $reqd }},omitempty{{ end }}"
{{- end }}

{{ define "query-tag" }}
{{- $key := get . "key" }}query:"{{ $key }}"
{{- end }}

{{ define "validate-tag" }}
{{- $ref := get . "ref" }}
{{- $reqd := get . "required" }}
{{- $components := get . "components" }}validate:"{{ include "validate-ref-rule" (dict "ref" $ref "required" $reqd "components" $components) }}"
{{- end }}


{{ define "validate-ref-rule" }}
{{- $ref := get . "ref" }}
{{- $reqd := get . "required" }}
{{- $components := get . "components" }}
{{- $rules := list }}
{{- if $reqd }}
  {{- $rules = append $rules "required" }}
{{- else }}
  {{- $rules = append $rules "omitempty" }}
{{- end }}
{{- $schema := $ref.Value }}
{{- if and (not $schema) (hasPrefix "#/components/schemas/" $ref.Ref) }}
  {{- $refSchema := index $components.Schemas (splitList "/" $ref.Ref | last) }}
  {{- if and $refSchema (eq $refSchema.Type "string") }}
    {{- $schema = $refSchema }}
  {{- end }}
{{- end }}
{{- with $schema }}
  {{- with get .Extensions "x-validate-override-tags" }}
    {{- $rules = (list .) }}
  {{- else }}
    {{- $childRules := include "validate-schema-rule" (dict "schema" . "components" $components) }}
    {{- if ne $childRules "" }}
      {{- $rules = append $rules $childRules }}
    {{- end }}
  {{- end }}
{{- end }}{{ join "," $rules }}
{{- end }}


{{ define "validate-schema-rule" }}
{{- $schema := get . "schema" }}
{{- $components := get . "components" }}
{{- $rules := list }}
{{- if eq $schema.Type "string" }}
  {{- if eq $schema.Format "uuid" }}
  {{- $rules = append $rules "uuid" }}
  {{- else if eq $schema.Format "email" }}
  {{- $rules = append $rules "email,lowercase" }}
  {{- else if eq $schema.Format "date-time" }}
  {{- /* Nothing to do, JSON decode will fail if not valid */}}
  {{- end }}
  {{- with $schema.Enum }}
  {{- $rules = append $rules (list "oneof=" (. | join " ") | join "") }}
  {{- end }}
  {{- if $schema.MaxLength }}
  {{- $rules = append $rules (cat "max=" ($schema.MaxLength | deref) | nospace) }}
  {{- end }}
  {{- if $schema.MinLength }}
  {{- $rules = append $rules (cat "min=" ($schema.MinLength | deref) | nospace) }}
  {{- end }}
{{- else if eq $schema.Type "integer" }}
  {{- if $schema.Maximum }}
  {{- $rules = append $rules (cat "lte=" ($schema.Maximum | deref) | nospace) }}
  {{- end }}
  {{- if $schema.Minimum }}
  {{- $rules = append $rules (cat "gte=" ($schema.Minimum | deref) | nospace) }}
  {{- end }}
  {{- if $schema.ExclusiveMaximum }}
  {{- $rules = append $rules (cat "lt=" ($schema.ExclusiveMaximum | deref) | nospace) }}
  {{- end }}
  {{- if $schema.ExclusiveMinimum }}
  {{- $rules = append $rules (cat "gt=" ($schema.ExclusiveMinimum | deref) | nospace) }}
  {{- end }}
{{- else if eq $schema.Type "array" }}
  {{- $rules = append $rules "dive" }}
  {{- $rules = append $rules (include "validate-ref-rule" (dict "ref" $schema.Items "required" true "components" $components)) }}
{{- end }}
{{- with get $schema.Extensions "x-validate-extra-tags" }}
  {{- $rules = append $rules . }}
{{- end }}{{ join "," $rules }}
{{- end }}
